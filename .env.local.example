# Local developer override — copy to .env.local and uncomment what you need.
# Only consulted in unpackaged (npm run dev) runs; overrides .env.development.
# Use this to point at a backend running on localhost instead of integration.

# VITE_API_URL=http://localhost:3001/api
# VITE_FRONTEND_URL=http://localhost:3000
